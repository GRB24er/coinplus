{
  "name": "realtime-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Realtime fan-out hub: pools upstream CoinGecko WebSocket connections and broadcasts to browsers.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/subscription-registry.test.ts"
  },
  "dependencies": {
    "tsx": "^4.20.6",
    "ws": "^8.18.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.0"
  }
}
